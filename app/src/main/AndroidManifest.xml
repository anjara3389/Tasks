<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="proyectohabitos.example.neita.habitos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FrmTasks">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddTask" />
        <activity android:name=".Chronometer" />
        <activity android:name=".Statistics" />
        <activity android:name=".WeeklyStatistics" />
        <activity android:name=".MonthlyStatistics" />
        <activity android:name=".GlobalStatistics" />
        <activity android:name=".NumPickersDialogFragment"></activity>
    </application>

</manifest>